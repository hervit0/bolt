version: '2'
options:
  verbose: true
syncs:
  bolt-sync:
    src: .
    sync_args:
      - '-prefer newer'
    sync_excludes:
      - tmp
      - .git
      - .git-crypt
      - log
      - vendor
      - vendor.orig
      - .docker-sync
